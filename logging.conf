[loggers]
keys=root,get_all_message_from_slack

[handlers]
keys=consoleHandler,fileHandler

[formatters]
keys=simpleFormatter

[logger_root]
level=DEBUG
handlers=consoleHandler,fileHandler

[logger_get_all_message_from_slack]
level=DEBUG
handlers=consoleHandler,fileHandler
qualname=get_all_message_from_slack
propagate=0

[handler_consoleHandler]
class=StreamHandler
level=DEBUG
formatter=simpleFormatter
args=(sys.stdout,)

[handler_fileHandler]
class=logging.handlers.TimedRotatingFileHandler
level=DEBUG
formatter=simpleFormatter
args=('logs/python.log', 'MIDNIGHT', 1)

[formatter_simpleFormatter]
format=%(levelname)-8s %(asctime)s %(module)s %(process)s %(pathname)s:%(lineno)d %(message)s
